// App-wide configuration
// Centralized place for all configurable values

const appConfig = {
  // Delete confirmation settings
  deleteConfirmation: {
    // Delay in seconds before the delete button becomes enabled
    buttonEnableDelay: 3,
  },
}

export default appConfig
